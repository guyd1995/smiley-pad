package com.dar.guy.smileypad

import androidx.annotation.WorkerThread
import androidx.camera.core.ImageProxy

class EmojiModeling {
    companion object {
        @WorkerThread
        fun analyzeImage(image: ImageProxy?, rotationDegrees: Int): R? {
            return null;
        }
    }

}